/**
 * USAGov's official directory of state motor-vehicle agencies. Used wherever a state's own
 * `dmv_website_url` hasn't been published yet, so a "find your DMV" / "book an appointment" link
 * still lands somewhere correct instead of disappearing. The page lets the visitor pick their
 * state and hands them their agency's real site.
 */
export const DMV_DIRECTORY_URL = "https://www.usa.gov/motor-vehicle-services";
