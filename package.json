{
  "name": "driving-test-app",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "dev:local": "concurrently -n API,WEB -c blue,magenta \"cd apps/api && php artisan serve --port=8001\" \"cd apps/web && pnpm dev\"",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "turbo": "^2.3.0"
  },
  "packageManager": "pnpm@10.30.3"
}
